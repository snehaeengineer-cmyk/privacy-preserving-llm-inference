# System X: Privacy-Preserving LLM Inference
# Install with: pip install -r requirements.txt

# ── PII Detection ──────────────────────────────────────────
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0

# spaCy + English model
spacy>=3.7.0
# After install, run: python -m spacy download en_core_web_lg

# SciSpaCy + Biomedical NER (BC5CDR corpus)
scispacy>=0.5.3
# After install, run:
# pip install https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.3/en_ner_bc5cdr_md-0.5.3.tar.gz

# ── LLM API (use one or both) ──────────────────────────────
anthropic>=0.25.0
openai>=1.30.0

# ── Evaluation ─────────────────────────────────────────────
sentence-transformers>=2.7.0   # semantic similarity scoring
scikit-learn>=1.3.0            # precision / recall / F1
numpy>=1.24.0
pandas>=2.0.0

# ── Utilities ──────────────────────────────────────────────
python-dotenv>=1.0.0           # load API keys from .env file
requests>=2.31.0               # HTTP calls to LLM gateway
pydantic>=2.0.0                # data schema / validation

# ── Notebook (optional) ────────────────────────────────────
jupyter>=1.0.0
ipykernel>=6.0.0
